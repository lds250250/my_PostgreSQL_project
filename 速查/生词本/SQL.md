# 生词本 · SQL

> **收什么**：SQL 语言本身的词 —— 关键字、函数名、类型名、约束名。
> **不收**：PostgreSQL 特有的命令和配置（去 `PostgreSQL.md`）。
> **规则**：见 `教学/如何教我.md` 的 `10.5 生词本`。
>
> ⚠️ **这本现在几乎是空的** —— 因为到 Day 1 为止，我们只写过一句真 SQL。
> Day 2 起才会真正往里装东西。

---

## 查询

| 代码里的词 | 中文短名 | 一句话 |
| --- | --- | --- |
| `SELECT` | 查询 | 从表里挑数据出来 |
| `version()` | 版本函数 | 返回数据库版本的那一行文字 |

---

## 待补（按 README 的排期）

| 哪天 | 会进这本的词 |
| --- | --- |
| Day 9 起 | `CREATE TABLE` / `ALTER TABLE` / `DROP TABLE` / `IF NOT EXISTS` |
| Day 10–11 | `NOT NULL` / `DEFAULT` / `UNIQUE` / `CHECK` / `PRIMARY KEY` / `FOREIGN KEY` |
| Day 15 起 | `INSERT` |
| Day 17–18 | `UPDATE` / `DELETE` / `RETURNING` / `TRUNCATE` |
| Day 24–25 | `WHERE` / `BETWEEN` / `IN` / `LIKE` / `ILIKE` / `ORDER BY` / `LIMIT` |
| Day 26–27 | `NULL` / `IS NULL` / `COALESCE` / `NULLIF` |
| Day 30 起 | `JOIN` 系列 |
| Day 36 起 | `GROUP BY` / `HAVING` / `COUNT` / `EXISTS` / `WITH` / 窗口函数 |
