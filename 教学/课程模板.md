# 课程模板

> **这是给"要给我上课的人"的材料。规矩在 `如何教我.md` 里，这份只管"复制粘贴"。**
>
> **⚠️ 但别急着填 —— md 在这节课真正结束之后才写。**
> 讲的时候我们还在交流，路径、输出、报错都可能是猜的。等我说"这一课可以了"，再按讨论后的真实情况落文件。
> 规则见 `如何教我.md` 的 `7.8 md 什么时候写`。

---

## 怎么用

**课讲完、讨论完，把最下面那一整块复制出来，往里填。**

**动笔前先过自检闸门**（`如何教我.md` 的 `3.1`）—— 数不出来的课，不要写。

**五条规矩先记住：**

1. **六个环节可以加，不能减。**
   某一环节今天确实没有内容，也要写一行说明"为什么没有"，**不要默默删掉。**
2. **提交环节如果没有文件改动**，就写一句"今天没有文件改动，不提交"，**同样不要默默跳过。**
3. **一节课里不止一个概念**，就在每个环节内部用「一、」「二、」分开 ——
   不要拆成好几节课，也不要把六个环节重复好几遍。
4. **一节课只推翻一个错误理解**（见 `如何教我.md` 的 `4.1`）。有两个以上就该拆天。
5. **不要停下来等我作答。** 问题当场给答案，课末给一张自检表让我自己对照（见 `2.4`）。

**关于开头那块"本课的点"：**

它**不是第七个环节**，是六个环节之前的标牌 —— 来自 `如何教我.md` 的 `7.3`（每个点标出类别）。
先贴标牌，再进六环节。**重点 / 难点 / 普通点**三样都要有，没有就写"无"，说清为什么无。

**不在模板里的几样东西：**

| 不放进课里的 | 放哪 |
| --- | --- |
| 生词 | `速查/生词本/` 的分册（`PostgreSQL.md` / `PowerShell.md` / `SQL.md` …） |
| "留一个问题"、"讲师备注"、"环境限制"这类 | 属于对话，不属于课程档案 —— **不要写进 md** |
| 讲法技巧（打比方、用数字、反直觉） | 属于老师的手艺，见 `如何教我.md` 附录 B |

**最容易漏的四处，先点名：**

| 环节 | 最容易漏的 |
| --- | --- |
| 一 | **"我原来会怎么想、错在哪"** —— 不说旧模型，后面的实验就退化成低阶实验 |
| 三 | **当场给答案** —— 只提问不回答，课就变成考试 |
| 四 | **"为什么这样设计"** —— 只讲语法就变成操作手册 |
| 六 | **自检表 + 心智模型 + 判断规则 + 能力总结** —— 四个都要，缺一个课就不完整 |

---

## 空模板

````markdown
# 第 X 课:（标题）

> **本课对应**：`README.md` 第 X 周 Day X
> **前置**：（上一课，或"无"）
> **状态**：⏳ 未开始 / ✅ 已完成（日期）
> **读完本课能做出什么**：（一句话，就是这一课要建立的模型）
> **本课产出的长期文件**：（没有就写"无"）

**本课的点**

- 重点:
- 难点:
- 普通点:

**今天唯一要形成的模型：**

> （一句话）

---

## 一、今天解决什么问题

**（一件具体的、能想象出画面的事 —— 让我觉得"这确实是个问题"）**

**今天学的就是这个 —— 它叫（名字，`代码里的写法`）。**

**一句话说:（日常语言的解释）。**

**你可能原来是这样理解的:（旧模型）**

**但这个理解错在哪:（一句话点破）**

**学完你能做出什么:（一句具体的话）。**

---

## 二、动手实验

### 0. 先检查环境

**（不要假设环境是对的。给出检查命令 + 结果对照表 + 按结果分支）**

| 检查结果 | 下一步 |
| --- | --- |
|  |  |

### 1. 今天的最小工具箱

| 命令 | 干什么 | 谁处理 |
| --- | --- | --- |
|  |  |  |

### 2. 实验 A：正常路径

```（语言）
（完整命令，每行上面跟一行注释）
```

**这里应该出现一个让我"咦"的现象：（是什么）**

### 3. 实验 B / C / D：故意制造的错误

**每个错误都要写清它想打破哪个理解。**

| 实验 | 错误类型 | 它想打破的理解 |
| --- | --- | --- |
| B | 环境错误 |  |
| C | 参数错误 |  |
| D | 用户错误 |  |

---

## 三、观察现象和解释

**（不要停下来等我回答。先问，紧接着自己答。）**

**先自己想一下：（1–3 个关键问题，不要多）**

**答案是：（当场给出，不中断）**

**常见误解 → 更正：**

| 大多数人会以为 | 其实是 |
| --- | --- |
|  |  |

**观察重点:（看什么，尤其是"看外形"这类提示）**

---

## 四、解释原理

**（一）它怎么工作**

**（每条命令讲三层：谁处理它 / 出错时谁报错 / 怎么从错误反推原因）**

**（二）为什么这样设计**

```
存在的问题
  ↓
为什么旧的做法不够用
  ↓
这个设计解决了什么
  ↓
它带来了什么新代价
```

**（三）好处和代价**

| 用了它，好处是什么 | 用了它，代价是什么 |
| --- | --- |
|  |  |

**什么条件下该换别的做法:**

---

## 五、提炼判断规律

**判断规则（要有顺序）:**

```
看到现象 A
  ↓
先检查 B
  ↓
如果不是，再检查 C
```

**支撑它的对照表:**

| 现象 | 谁报的 | 到哪一层了 | 先查什么 |
| --- | --- | --- | --- |
|  |  |  |  |

**边界:（这个结论在什么条件下成立）**

---

## 六、实践记录和总结

### 提交

```
git add .
git commit -m "第X课：______"
git push
```

### 总结、回顾

**今天学了什么:**

**和前面什么关系:**

### 心智模型

**（用一句话说出：这个系统是怎么组织的）**

### 今天以后遇到什么问题，我可以独立判断

| 以前我会怎么做 | 现在我能怎么做 |
| --- | --- |
|  |  |

---

**自检表**

> 先自己想一下，再看答案。**想错的，回中间那列指的那一节重看。**

| 问题 | 答案 | 在哪一节讲的 |
| --- | --- | --- |
|  |  |  |

---

**产出物清单**

| 文件 | 干什么 | 做完能看到 |
| --- | --- | --- |
|  |  |  |
````

---

## 每一段具体怎么写

**空模板只管"有哪些格子"。每个格子该填成什么样，看 `如何教我.md`：**

| 环节 | 看哪里 |
| --- | --- |
| 一 | `2.0 先定：这节课要推翻什么`、`3. 单课模板` |
| 二 | `4.3 每节课必须有三类错误`、`5.3 工具箱要前置`、`6.4 不要假设环境永远正确` |
| 三 | `2.2 先实验后解释`、`2.4 提问要自问自答`、`4.4 实验后的保存点` |
| 四 | `5.1 命令要讲三层`、`2.3 培养判断能力` |
| 五 | `6.3 产出一条能复用的判断规则` |
| 六 | `8.1 Git 提交`、`2.0 心智模型`、`9.3 用产出物验收` |

**讲法上想不出怎么写，翻 `附录 A：表达示例` 和 `附录 B：讲法技巧`。**
